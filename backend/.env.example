# Base de datos
DB_USER=sa
DB_PASSWORD=<tu-password-segura>
DB_NAME=reservas
DB_SERVER=db
DB_PORT=1433

# Auth
JWT_SECRET_KEY=<clave-aleatoria-larga-min-32-caracteres>

# Entorno
APP_ENV=development

# Script seed (solo para crear primer admin — ejecutar una sola vez)
SEED_EMAIL=<email-del-admin>
SEED_PASSWORD=<password-del-admin>
